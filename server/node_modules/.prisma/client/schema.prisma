generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Dessert {
  id        Int    @id @default(autoincrement())
  name      String
  category  String
  price     Float
  thumbnail String
  mobile    String
  tablet    String
  desktop   String
}
